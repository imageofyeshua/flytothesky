<div class="w-full">
  <%= image_tag(listing.images.first, class: "h-64 rounded-lg overflow-hidden object-cover") if listing.images.any? %>
  <p class="text-lg font-bold"><%= listing.title %></p>
</div>